# Журнал архитектурных решений

## ADR-001

**Решение:** T-one проверяется как основной кандидат; whisper.cpp остаётся резервом.  
**Дата:** 2026-08-09  
**Статус:** принято для этапа исследования  
**Контекст:** нужен локальный ASR русской телефонной речи; обучение модели с нуля запрещено.  
**Выбранный вариант:** сначала проверить официальный T-one и его предобученные веса.  
**Альтернативы:** whisper.cpp; другие локальные ASR не рассматриваются до появления причины отказаться от T-one.  
**Причина:** T-one заявлен как потоковая модель, специализированная для русской телефонии, и прямо указан заказчиком как основной кандидат.  
**Последствия:** прототип не должен преждевременно связывать доменную логику с API модели; нужен адаптер и проверка локальной загрузки.

## ADR-002

**Решение:** разделить подготовку артефактов и runtime-инференс.  
**Дата:** 2026-08-09  
**Статус:** принято  
**Контекст:** зависимости и веса можно получить первоначально, но после установки сервис обязан работать без интернета.  
**Выбранный вариант:** отдельная документированная staging-процедура формирует локальный каталог модели и манифест контрольных сумм; runtime не скачивает ничего.  
**Альтернативы:** разрешить библиотеке заполнять кэш при первом звонке.  
**Причина:** первый runtime-вызов с автоскачиванием нарушает автономность и затрудняет воспроизводимость.  
**Последствия:** CLI завершится явной ошибкой при отсутствии локальной модели; автоматического fallback не будет.

## ADR-003

**Решение:** очередь и watcher реализуются только после успешной обработки одного искусственного файла.  
**Дата:** 2026-08-09  
**Статус:** принято  
**Контекст:** преждевременная очередь усложнит диагностику модели, качества и автономности.  
**Выбранный вариант:** сначала ручной CLI и стабильный JSON-контракт.  
**Альтернативы:** сразу создать демон и API.  
**Причина:** соответствует критическому пути и ограничивает объём первой проверки.  
**Последствия:** фоновые режимы остаются запланированными, но не считаются частью первого доказательства.

## ADR-004

**Решение:** использовать только локальный конструктор T-one и собственный CLI; upstream web demo/готовый Docker-образ исключить.  
**Дата:** 2026-08-09  
**Статус:** принято после статического аудита  
**Контекст:** upstream предоставляет `from_local`, но demo при отсутствии переменной окружения вызывает Hugging Face, а браузер загружает JavaScript/CSS с внешнего CDN. Docker build также скачивает installer и веса.  
**Выбранный вариант:** проверенные локальные артефакты, `StreamingCTCPipeline.from_local`, offline/telemetry-disable flags, собственная CLI-обвязка без web-зависимостей.  
**Альтернативы:** официальный demo, Docker Hub image, `from_hugging_face()` в runtime.  
**Причина:** локальный API минимален, проверяем и не требует сети в видимом коде.  
**Последствия:** нужно отдельно подготовить модель и зависимости; отсутствие файлов является ошибкой; сеть всё равно блокируется на уровне ОС.

## ADR-005

**Решение:** начать фактический тест с одной задачи и не выбирать параллельность до измерений.  
**Дата:** 2026-08-09  
**Статус:** принято  
**Контекст:** текущая машина имеет 2 CPU/8 ГБ, а upstream рекомендует 4 CPU/8 ГБ; `kenlm.bin` занимает около 5,46 ГБ.  
**Выбранный вариант:** одиночный инференс, измерение RSS/RTF; очередь и воркеры позже.  
**Альтернативы:** сразу параллельные тесты или Triton/TensorRT.  
**Причина:** не допустить OOM и не смешивать проверку функциональности с масштабированием.  
**Последствия:** данные текущего ноутбука не являются production sizing; beam search может потребовать более подходящую машину.

## ADR-006

**Решение:** использовать SQLite/WAL, транзакционный claim и продлеваемый lease для локальной очереди.  
**Дата:** 2026-08-09  
**Статус:** принято и реализовано  
**Контекст:** после успешного одиночного теста требуются восстановление после перезапуска, запрет двойной обработки и ограниченные retry без внешней инфраструктуры.  
**Выбранный вариант:** одна SQLite-база, статусы `new/queued/processing/completed/failed`, стабильность size/mtime, `BEGIN IMMEDIATE`, worker UUID, heartbeat и exponential backoff.  
**Альтернативы:** файлы-маркеры; Redis/RabbitMQ; сразу несколько параллельных воркеров.  
**Причина:** SQLite локален, транзакционен, не требует сетевого сервиса и достаточен для MVP.  
**Последствия:** база должна находиться на файловой системе с корректной SQLite locking semantics; NFS/SMB требуют отдельной проверки. Масштабирование за пределы одного узла потребует нового ADR.

## ADR-007

**Решение:** первый фактический тест выполнить с greedy decoder, не выдавая его качество за beam-search качество T-one.  
**Дата:** 2026-08-09  
**Статус:** временно принято  
**Контекст:** `model.onnx` занимает 144 МБ, `kenlm.bin` — 5 463 477 004 байта; на тестовом хосте 8 ГБ RAM и около 12 ГБ свободного диска.  
**Выбранный вариант:** доказать локальный pipeline на greedy, сохранить параметр `--decoder beam_search` и строгую проверку KenLM.  
**Альтернативы:** сразу скачать/загрузить KenLM; перейти на whisper.cpp.  
**Причина:** ограничить риск исчерпания диска/RAM и сначала проверить интеграционный контракт.  
**Последствия:** WER `0,1930` синтетического теста относится только к greedy; окончательное решение по T-one ожидает beam-search и естественный набор.

## ADR-008

**Решение:** для проверенного Windows MVP использовать hash-pinned CPython 3.11 wheelhouse и greedy без Python binding KenLM; исходящий трафик runtime блокировать отдельным firewall-правилом.  
**Дата:** 2026-08-09  
**Статус:** принято и реализовано для Windows greedy  
**Контекст:** официального Windows wheel `kenlm==0.3.0` нет, Visual C++ build tools на хосте отсутствуют, но upstream `tone==0.1.0` объявляет KenLM обязательным и импортирует `pyctcdecode` даже для фактически независимого greedy decoder-а. Непроверенный бинарник или большая локальная toolchain увеличат риск цепочки поставки.  
**Выбранный вариант:** зафиксировать Windows wheels и SHA-256 в `requirements/windows-greedy.txt`, собрать неизменённый официальный T-one wheel локально, установить его с `--no-deps`, оставить строгую проверку отсутствующего `kenlm.bin` для beam search и подавить только ожидаемое предупреждение PyCTCDecode в greedy-режиме. После staging применяется `security/windows-deny-network.ps1`.  
**Альтернативы:** собирать KenLM из sdist; брать неофициальный Windows wheel; патчить upstream packaging/импорты; перейти на whisper.cpp.  
**Причина:** минимальный проверяемый путь даёт рабочий быстрый локальный greedy без непроверенных бинарников и не меняет код аудита T-one.  
**Последствия:** `pip check` сообщает известное отсутствие `tone -> kenlm`; это не допускается выдавать за готовый beam runtime. Для beam search потребуется проверенная сборка KenLM, локальный `kenlm.bin` и новый тест. Firewall-скрипт требует административных прав и не был применён в текущей ограниченной сессии.

## ADR-009

**Решение:** Windows worker измеряет CPU через `time.process_time()`, а недоступный peak RSS явно печатает как `unavailable`; новую runtime-зависимость только ради метрики не добавлять.  
**Дата:** 2026-08-09  
**Статус:** принято и реализовано  
**Контекст:** `worker_cli.py` безусловно импортировал Unix-only модуль `resource`, из-за чего worker не запускался на Windows.  
**Выбранный вариант:** условный импорт `resource`; Linux/macOS сохраняют прежние CPU/RSS-метрики, Windows сохраняет wall/CPU и честно не заявляет RSS.  
**Альтернативы:** добавить `psutil`; реализовать Windows Process Status API через `ctypes`; отказаться от Windows worker.  
**Причина:** метрика памяти не должна блокировать обработку и не оправдывает новую зависимость до performance-этапа.  
**Последствия:** для Windows capacity planning peak RSS нужно отдельно измерить штатным мониторингом ОС или после обоснованного добавления проверенной зависимости.

## ADR-010

**Решение:** переносить проект между Windows и macOS через приватный GitHub-репозиторий только с исходным кодом и безопасными manifests.  
**Дата:** 2026-08-10  
**Статус:** принято и реализовано  
**Контекст:** владельцу нужен переносимый репозиторий, но модель, аудио, транскрипции, логи, окружения и staging-кэши нельзя публиковать или смешивать с исходниками.  
**Выбранный вариант:** приватный `gurulow1/local-call-transcriber`; `.gitignore` исключает `models/*`, фактические `data/*`, `logs/*`, `.venv/`, `.poetry-cache/` и `third_party/`. На новом Mac зависимости, официальный pinned checkout T-one и модель подготавливаются заново по `docs/INSTALLATION.md`.  
**Альтернативы:** публичный репозиторий; Git LFS для модели/аудио; архив всей рабочей папки; включение vendored T-one.  
**Причина:** минимизировать риск раскрытия данных и не превращать GitHub в хранилище тяжёлых или приватных runtime-артефактов.  
**Последствия:** одного `git clone` недостаточно для инференса; на каждом хосте нужен контролируемый staging и локальная проверка весов. Доступ к приватному репозиторию требуется настроить отдельно на Mac.

## ADR-011

**Решение:** добавить AAC ADTS через опциональный hash-pinned PyAV 18.0.0, не заявляя поддержку всех AAC-профилей.
**Дата:** 2026-08-10
**Статус:** принято и реализовано для локального MVP
**Контекст:** miniaudio 1.61 не декодирует AAC, а встроенный macOS `afconvert` не открыл фактический AAC v2 LC ADTS. Нужен одинаковый локальный декодер на Intel macOS и Windows x64.
**Выбранный вариант:** PyAV 18.0.0 из официальных PyPI wheels, зафиксированных двумя platform-specific SHA-256. Runtime открывает только локальный regular file с явным AAC demuxer, сводит в mono signed 16-bit 8 кГц и не меняет источник. Единичные невалидные packets пропускаются с техническим warning; файл без декодируемых frames явно отклоняется.
**Альтернативы:** ложно разрешить `.aac` без decoder; внешний `ffmpeg` binary; platform-specific `afconvert`; предварительная ручная конвертация.
**Причина:** бинарные wheels есть для обеих проверенных платформ, кодек работает без отдельной системной установки и может быть перенесён во внутренний wheelhouse.
**Последствия:** AAC требует CPython 3.11+ и отдельный wheel; бундл FFmpeg увеличивает supply-chain и attack surface. До production нужны SCA/CVE, SBOM и license review конкретного wheel.

## ADR-012

**Решение:** очищать текст детерминированными локальными правилами и словарём, всегда сохраняя исходную выдачу ASR.
**Дата:** 2026-08-10
**Статус:** принято и реализовано для локального MVP
**Контекст:** greedy T-one возвращает строчные сегменты без пунктуации и ошибается в доменных терминах; свободная LLM-редактура может незаметно изменить суммы, отрицания и имена.
**Выбранный вариант:** `config/glossary.json` хранит только канонические написания, не добавляющие смысл. Постобработка нормализует пробелы, капитализирует начало транскрипта и текст после явного конца предложения, а финальный знак добавляет только в конец всей транскрипции. Произвольная граница ASR-сегмента не считается границей предложения. JSON 1.1 хранит очищенные `text`/`segments[].text`, исходные `raw_text`/`segments[].asr_text` и метаданные правил.
**Альтернативы:** не менять ASR-текст; подключить локальную LLM; потерять raw ASR после замены.
**Причина:** правила воспроизводимы, проверяемы тестами, не требуют новой модели и не передают данные.
**Последствия:** словарь становится управляемым артефактом и должен проходить проверку на размеченном наборе; точечная замена может быть неверной в другом контексте. LLM, диаризация и семантическая коррекция остаются вне текущего MVP.

## ADR-013

**Решение:** хранить каждый звонок в самодостаточной папке `data/calls/{call_id}/` с аудио, JSON и Markdown.
**Дата:** 2026-08-10
**Статус:** принято и реализовано
**Контекст:** плоские `data/input` и `data/output` накапливали много файлов и усложняли сопоставление аудио с результатами.
**Выбранный вариант:** watcher создаёт папку по `call_id`, после проверки стабильности перемещает туда тот же аудиофайл без перекодирования и публикует `{call_id}.json` и `{call_id}.md`. Очередь обновляет путь и может восстановиться, если процесс прерван между переносом и обновлением SQLite.
**Альтернативы:** оставить плоские каталоги; копировать аудио и создавать дубль; оставить только JSON.
**Причина:** одна папка даёт понятную единицу хранения, Markdown удобен для человека, JSON — для машинной интеграции.
**Последствия:** `data/input` является только staging-папкой; повтор того же `call_id` запрещён без явной политики ревизий. Все три файла имеют одинаковые ограничения доступа и хранения.

## ADR-014

**Решение:** до получения конкретных требований CRM поставлять нейтральный HTTP-адаптер, доступный только через loopback.
**Дата:** 2026-08-10
**Статус:** принято и реализовано как reference integration
**Контекст:** транскрибатор должен быть встроен технической командой в существующую CRM, но её API, аутентификация, сеть и способ хранения неизвестны.
**Выбранный вариант:** raw-body `PUT` помещает аудио в существующий staging, `GET` возвращает статус или готовый JSON. Адаптер использует тот же SQLite-worker, не дублирует ASR и не добавляет runtime-зависимости.
**Альтернативы:** только файловый обмен; сразу выдумать CRM-специфичный plugin; открыть HTTP в локальную сеть без защиты.
**Причина:** три простых endpoint показывают границу интеграции и дают технарям исполняемый эталон, не принимая за них инфраструктурные решения.
**Последствия:** reference API не имеет TLS/аутентификации и не может публиковаться в сеть. Боевой адаптер должен быть встроен за корпоративным mTLS/service-auth proxy или заменён на нативный адаптер CRM.

## ADR-015

**Решение:** сделать `whisper.cpp v1.9.2` с `Whisper large-v3` и beam search основным quality-движком на проверенном Windows/NVIDIA-хосте; T-one/greedy оставить лёгким fallback.

**Дата:** 2026-08-11

**Статус:** принято и реализовано для локального пилота

**Контекст:** T-one/greedy дал WER `0,1930` на единственном сложном TTS-эталоне, не расставляет пунктуацию и склеивает аббревиатуры; T-one beam search на Windows заблокирован отсутствием проверенного KenLM binding. Доступен RTX 3080 10 ГБ и достаточный диск.

**Выбранный вариант:** официальный Windows CUDA bundle whisper.cpp commit `306c88f4d1286aec1bf96e544632897886af5501`, модель GGML F16 `large-v3` SHA-256 `64d182b440b98d5203c4f9bd541544d84c605196c4f7b845dfa11fb23594d1e2`, только локальные пути и JSON через UTF-8 response file. Отдельная LLM не используется.

**Альтернативы:** собирать KenLM для T-one; faster-whisper/CTranslate2 и отдельные cuBLAS/cuDNN; облачный ASR; LLM-корректор.

**Причина:** whisper.cpp даёт проверяемый официальный бинарный runtime без новой Python-цепочки зависимостей, использует имеющуюся GPU и поддерживает локальный multilingual beam search. LLM-корректор может выдумывать реквизиты и не принимается без отдельной оценки.

**Последствия:** staging требует около 3,8 ГБ загрузок; текущий pinned bundle — Windows x64/NVIDIA. AAC перед Whisper локально декодируется в удаляемый временный mono PCM WAV 16 кГц, поэтому для AAC нужен зафиксированный PyAV wheel. На `1001.wav` критические БИК/ИНН/НКЦ/сумма/дата распознаны, но «четыре ноля и один» преобразовано в ошибочное `4001`, поэтому реквизиты требуют бизнес-валидации. Буквальный WER с эталоном, где числа записаны словами, нельзя сравнивать с T-one без согласованной нормализации чисел. Native `whisper-cli.exe` блокируется отдельным firewall-правилом наряду с Python.

## ADR-016

**Решение:** архив получает самоустанавливающиеся запускаторы для основных Windows/Linux и дополнительного macOS; платформенные оболочки подготавливают Python, а общий bootstrap устанавливает и запускает локальный ASR.

**Дата:** 2026-08-12

**Статус:** принято и реализовано для передачи локального пилота

**Контекст:** получатель не должен устанавливать Python, package manager, зависимости и модель вручную. Прежний `.command` только проверял готовую `.venv`, а Windows/Linux one-click пути отсутствовали. Основные целевые платформы — Windows и Linux; macOS нужен как дополнительный путь.

**Выбранный вариант:** `.cmd`/PowerShell, `.sh` и `.command` загружают project-local `uv 0.11.13` с зафиксированным SHA-256 и создают managed Python 3.12. Общий `scripts/bootstrap_runtime.py` на Windows с NVIDIA готовит Whisper large-v3/CUDA, а на Windows без NVIDIA, Linux x86_64 и macOS — hash-pinned T-one/greedy. Успешный staging отмечается marker; окружение, импорты, модель и manifest проверяются при каждом старте.

**Проверка новой зависимости:** `uv` используется только как staging/bootstrap-инструмент для управляемого Python и wheel-установки; источник — официальный release Astral на GitHub, лицензия — MIT или Apache-2.0, версия и платформенные архивы закреплены SHA-256. Сетевые обращения разрешены только в первом staging-запуске.

**Альтернативы:** требовать ручную установку; отправлять многогигабайтный готовый архив; собирать отдельные MSI/AppImage/подписанный `.app`; автоматически собирать whisper.cpp для каждой ОС.

**Причина:** один видимый файл на платформу даёт требуемый пользовательский сценарий без предварительного Python и системного package manager, сохраняя разделение staging/runtime и проверку ключевых загрузок.

**Последствия:** первый запуск требует интернет; Windows/NVIDIA скачивает около 3,8 ГБ, CPU-путь — несколько сотен мегабайт. Linux GUI может попросить включить флаг выполнения, macOS — показать стандартное предупреждение. Linux ARM не заявлен из-за отсутствия готового `miniaudio 1.61` wheel. Транзитивные PyPI wheels пока разрешаются из официального индекса и до production должны быть перенесены в полный hash-pinned внутренний wheelhouse.

## ADR-017

**Решение:** Windows one-click разделяет сетевой staging и запуск worker обязательным firewall-шагом; готовая модель проверяется по SHA-256 при каждом старте.

**Дата:** 2026-08-12

**Статус:** принято и реализовано для архивного Windows-пилота

**Контекст:** архивный bootstrap создаёт Python в `.poetry-cache\venv`, тогда как прежний firewall-скрипт был привязан к разработческой `.venv`. Кроме того, наличие marker, файла модели и manifest не обнаруживало повреждение весов с сохранённым именем.

**Выбранный вариант:** Windows bootstrap сначала завершает staging и локальные тесты, затем через стандартный UAC-запрос применяет outbound-block к фактическому управляемому Python и `whisper-cli.exe`, и только после успеха запускает worker. `_runtime_ready` вызывает локальный `verify_model.py`, который сверяет заявленные размер и SHA-256.

**Альтернативы:** оставить firewall ручным; запускать worker даже при отказе UAC; проверять только наличие файлов; читать 3,1 ГБ перед каждым отдельным аудиофайлом.

**Причина:** получатель должен получить безопасный порядок одним кликом, а повреждённый или подменённый вес не должен считаться готовым runtime. Проверка выполняется один раз при запуске worker-процесса, а не перед каждым звонком.

**Последствия:** Windows-получателю нужны административные права для firewall; при отказе worker не стартует. Повторный запуск Whisper дополнительно читает 3,1 ГБ модели для SHA-256, поэтому старт занимает больше времени. Полный first-run с NVIDIA всё равно должен быть подтверждён на реальном Windows/NVIDIA-хосте.

## ADR-018

**Решение:** для Whisper обязательно применять локальный hash-pinned Silero VAD и общий валидатор результата; при отсутствии VAD завершать запуск fail-closed.

**Дата:** 2026-08-13

**Статус:** принято и реализовано для пилота

**Контекст:** pinned Whisper large-v3 выдумывал 2–7 слов на 10 секундах тишины, белого шума и чистого тона. Кроме того, whisper.cpp мог вернуть конец сегмента `30,0` для файла длительностью `16,115` с.

**Выбранный вариант:** официальный `ggml-silero-v5.1.2.bin` из `ggml-org/whisper-vad`, revision `e5614ed76a5dd4b03fad5068c89efcd2617a9d1e`, размер 885098 байт, SHA-256 `29940d98d42b91fbd05ce489f3ecf7c72f0a42f027e4875919a28fb4c04ea2cf`; `whisper-cli` всегда получает `--vad --vad-model --vad-threshold 0.50`. Общий слой отклоняет non-finite, отрицательные, обратные и немонотонные таймкоды, а допустимый end-overhang ограничивает длительностью. Инференс ограничен четырьмя часами и duration-based timeout.

**Альтернативы:** только Whisper `no_speech_threshold`; простая energy gate; фильтрация текста после ASR; продолжать публиковать неречевые галлюцинации.

**Причина:** штатный VAD в pinned whisper.cpp возвращает пустой transcript при отсутствии речевых сегментов и не требует новой runtime-библиотеки. Простая энергия не отличает речь от тона/шума, а текстовый фильтр не может надёжно доказать отсутствие речи.

**Последствия:** VAD-модель становится обязательным проверяемым runtime-артефактом. На regression-наборе тишина/два уровня шума/тон дали 0 слов; шесть FLEURS сохранили 4/135 ошибок (micro-WER 2,96%), нарушения таймкодов отсутствовали. Это не заменяет испытания тихой речи и телефонных звонков.

## ADR-019

**Решение:** разделить публично воспроизводимый model-free CI и локальную приёмку полного ASR-контура с весами и открытым аудио.

**Дата:** 2026-08-14

**Статус:** принято и реализовано для пилота

**Контекст:** код, контракты, очередь и release-архив можно проверять без многогигабайтных моделей, но WER, VAD и GPU-path требуют локальных артефактов, которые запрещено помещать в Git. Отсутствие CI делало состояние GitHub-репозитория менее наглядным перед передачей.

**Выбранный вариант:** GitHub Actions запускает unit/contract suite и `compileall` на Windows и Linux с Python 3.11/3.12. Workflow имеет только `contents: read`; `actions/checkout` v7.0.1 и `actions/setup-python` v7.0.0 закреплены полными commit SHA. Реальные аудио, модели, результаты и логи в CI не передаются. Source-архив и source-tree hash включают сам workflow.

**Проверка новых зависимостей:** обе Actions поддерживаются организацией GitHub, публикуются под MIT и используются только во время CI. Python test-extra содержит уже принятые в runtime NumPy 1.26.4 и PyAV 18.0.0; новых библиотек обработки не добавлено.

**Альтернативы:** не иметь CI; загружать модели и benchmark-аудио в облачный runner; тестировать только одну ОС; использовать плавающие action tags.

**Причина:** короткий model-free gate ловит переносимость и контрактные регрессии при каждом push, не нарушая zero-egress границу рабочих данных и не превращая CI в второй runtime.

**Последствия:** зелёный CI не доказывает качество ASR или готовность firewall/GPU-хоста. Перед показом по-прежнему обязательны локальный preflight и evidence-приёмка из `docs/PILOT_REPORT.md`.
